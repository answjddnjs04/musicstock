# 📋 MusicStock — 전체 기획서 & 기능 지도

이 문서는 MusicStock이 지금까지 무엇을 만들었고, 왜 만들었고, 어디로 가고 있는지를 한 번에 파악하기 위한 문서입니다. 세부 구현은 각 소스 파일 상단 주석을 참고하세요.

## 1. 한 줄 정의

**MusicStock**은 음악의 실제 인기(스트리밍/조회수)를 가상 주식처럼 사고팔며, 그 인기가 만든 수익(배당)을 나눠 받는 **음악 팬덤 가상 트레이딩 플랫폼**입니다.

## 2. 우리가 추구하는 가치와 방향

- **팬심을 참여로 바꾼다.** "내가 좋아하는 아티스트가 잘 되면 좋겠다"는 마음을 응원 이상의 것으로 만듭니다 — 실제로 그 인기를 보유하고, 그 성장에서 보상을 받는 경험.
- **가짜 데이터가 아닌 진짜 인기를 반영한다.** 초기엔 목업 데이터로 화면을 검증했지만, 지금은 Spotify(정식 발매곡 판별) + YouTube(조회수) 기반 실제 데이터로만 마켓을 구성합니다.
- **희소성에서 시작해 점진적으로 넓힌다.** 곡마다 처음엔 주식을 1주만 발행합니다. 서비스가 커지고 유저가 늘어날수록 관리자가 주식분할 개수를 늘려 더 많은 사람이 참여할 수 있게 열어줍니다 — 처음부터 무제한 공급하지 않고, 신뢰와 희소가치를 먼저 쌓는 방향.
- **싸구려처럼 보이지 않는다.** 다크 신스 톤앤매너와 실시간으로 살아 움직이는 UI(전광판, 랭킹, 애니메이션)로 감성적인 완성도를 지킵니다.

## 3. 기능 피라미드

```
                        MusicStock
                            │
        ┌──────────┬────────┼────────┬──────────┬──────────┐
        │          │        │        │          │          │
     인증/계정     마켓   포트폴리오   관리자    실데이터    인프라/
                          &정산      도구      파이프라인    배포
```

아래 4장은 위 피라미드를 위(제품 전체) → 아래(세부 기능) 순서로 그대로 목차화한 것입니다.

## 4. 상세 목차

### 4.1 인증 & 계정
- 이메일/비밀번호 회원가입·로그인 (Supabase Auth)
- 로그인 세션 유지 (새로고침해도 유지), 로그아웃
- 관리자 계정 식별 (`infinitefoever@naver.com` 로그인 시에만 관리자 기능 노출)

### 4.2 마켓 (거래)
- **홈 화면**: 상단 헤더(마켓 타이틀 · 오늘의 수수료 · 내 잔고) + 검색창 + 탭 메뉴
- **랭킹 3종 (통합 패널)**: 주가 급상승 / 배당 수익률 / 시가총액 — 각각 1등 하이라이트 카드 + 2~8등 리스트가 하나의 패널로 이어짐
- **검색**: "검색 음악" 탭에서 곡명·아티스트명으로 실시간 필터링, 바로 매수/매도 가능
- **매수/매도 모달**: 수량 조절, 예상 수수료·총 결제금액 미리보기, 잔고/재고 부족 시 검증
- **재고(공급량) 제한**: 곡마다 정해진 주식 수(`total_shares`)를 초과해서 팔리지 않도록 서버에서 원자적으로 체크 — 품절 시 "품절" 표시
- **동적 거래 수수료**: 어제 지급된 배당금이 많을수록(=인플레이션) 오늘 수수료율이 올라감 (최대 5%)
- **시가총액**: 현재가 × 주식분할 개수로 계산

### 4.3 포트폴리오 & 정산
- **내 포트폴리오**: 보유 음원 리스트, 평단가, "오늘 밤 예상 배당금" 표시
- **배당금 로직**: 보유 수량 × 지분율 × 조회수 기반 계산
- **일일 정산**: 배당금 지급 + 다음 날 수수료율 갱신
- **접속 시 영수증 모달**: 어제 총 배당금, 최고 배당 수익 곡, 오늘 수수료율 요약. "오늘 하루 보지 않기"로 하루 동안 재노출 방지

### 4.4 관리자 도구 (관리자 계정 전용)
- **아티스트 등록**: 아티스트명 검색 → Spotify에서 정식 발매곡만 최대 5곡 추출(뮤직비디오/팬캠 등 노이즈 배제) → YouTube 조회수 매핑 → 관리자가 체크박스로 원하는 곡만 선택 → 등록
- **주가 관리**: 곡별로 "판매됨 X / 총 Y"를 보면서 주식분할 개수(`total_shares`)를 즉시 조정 — 유저가 늘어날 때 공급을 늘리는 창구

### 4.5 실데이터 파이프라인
- **Spotify 연동**: Client Credentials 플로우로 정식 트랙만 검색 (서버리스 함수에서만 호출, 키 노출 없음)
- **YouTube 연동**: Official Audio/Topic 채널 우선 매핑으로 조회수 수집, 앨범 커버 썸네일 추출
- **자동 폴백**: Spotify 호출이 실패하면(예: 앱 소유 계정 Premium 미구독) 자동으로 YouTube 단독 검색으로 전환해 기능이 끊기지 않도록 함
- **초기 주가 산정**: 조회수 × 계수로 최초 주가 결정, 등락률/배당률은 실거래 이력이 쌓이기 전까지 랜덤 초기값 부여

### 4.6 디자인 시스템
- **Dark Synth 팔레트**: 배경 `#0B0E14`, 카드 `#161B26`, 상승/배당 `#00FF87`, 하락 `#FF2E93`, 보조 텍스트 `#8A94A6`
- 반응형 레이아웃: 모바일은 세로 스택, 데스크톱은 넓은 화면을 3컬럼으로 활용

### 4.7 인프라 & 배포
- **프론트엔드**: React + Vite + Tailwind CSS
- **백엔드**: Supabase (Auth, Postgres, Row Level Security, RPC 함수)
- **API 프록시**: Cloudflare Pages Functions (Spotify/YouTube 키를 서버에서만 사용)
- **배포**: Cloudflare Pages (GitHub `musicstock` 저장소와 연동, push 시 자동 빌드)

## 5. 기술 스택 요약

| 영역 | 사용 기술 |
|---|---|
| UI | React 19, Tailwind CSS v4, Vite |
| 상태 관리 | React Context + useReducer |
| 인증/DB | Supabase (Auth, Postgres, RLS, RPC) |
| 외부 데이터 | Spotify Web API, YouTube Data API v3 |
| 배포 | Cloudflare Pages + Pages Functions |

## 6. 앞으로의 확장 지점

- 관심 음악(찜) 기능
- 거래 목록(매매 히스토리) 화면
- 배당금 계산을 "당일 조회수 증가분" 기준으로 정교화 (현재는 누적 조회수 기반)
- 주식분할 시 기존 보유자 지분 자동 환산
